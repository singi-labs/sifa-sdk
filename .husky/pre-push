# pre-commit already runs format:check + lint + typecheck.
# build catches a broken publishable bundle before push.
pnpm build || exit 1

echo "CodeRabbit review (advisory, non-blocking)…"
coderabbit review --plain || true
