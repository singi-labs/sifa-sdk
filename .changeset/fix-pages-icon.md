---
'@singi-labs/sifa-sdk': patch
---

Fix `Pages` category icon: `BrowserSimple` doesn't exist in `@phosphor-icons/react`. Use `Browser` instead.

Caught during the sifa-web rewrite (PR 2 of the category-taxonomy series).
