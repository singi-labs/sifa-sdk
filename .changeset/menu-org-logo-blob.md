---
'@singi-labs/sifa-sdk': patch
---

Add `orgLogoBlob` to `AccountSummary` so the account switcher can resolve a company account's uploaded logo through the shared company-logo resolution, matching its `/c/` page.
