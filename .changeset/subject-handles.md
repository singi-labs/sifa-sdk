---
'@singi-labs/sifa-sdk': patch
---

Add `subjectHandle` and `subjectName` to `RepoRecordLabel` so a caller can attach a resolved identity to records whose whole meaning is a relationship, name an untitled talk by its event, and give `id.sifa.meeting` a subject. A page listing 33 follows previously showed 33 identical rows over a column of `did:plc:` strings.
