---
'@singi-labs/sifa-sdk': patch
---

Support courses the user taught: `role`, `startedAt`, `endedAt` and `position` on the course record and write schemas and on `ProfileCourse` / `CourseView`, plus the course-role taxonomy (`COURSE_ROLE_OPTIONS`, `getCourseRoleLabel`, `isTeachingCourse`, `splitCoursesByRole`).
