---
'@singi-labs/sifa-sdk': patch
---

Link versions of a publication: `relatedIdentifiers` on the publication record and write schemas and on `ProfilePublication` (DataCite relation vocabulary), and `groupPublicationVersions`, which folds a preprint under its published version and older releases under the newest.
