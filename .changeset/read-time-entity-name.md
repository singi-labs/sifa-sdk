---
'@singi-labs/sifa-sdk': patch
---

Add `entityName` to `ProfilePosition`: the resolved current canonical name of a durably-linked company, so consumers can render it over the stored `company` label for linked positions (#240).
