---
'@singi-labs/sifa-sdk': patch
---

Add recipe.exchange (`exchange.recipe.recipe`) to the app category map and URL patterns. Recipes link to the recipe.exchange viewer by rkey; the profile fallback points at the per-handle author page.
