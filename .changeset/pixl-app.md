---
'@singi-labs/sifa-sdk': patch
---

Add Pixl (`pics.pixl.image`) to the app category map (Photos) and `COLLECTION_TO_APP`. Pixl (pixl.pics) is a photo-sharing app; it is auth-gated with no public per-record viewer, so no URL pattern is registered and its cards render non-clickable.
