---
"@singi-labs/sifa-sdk": patch
---

Add the owner-editable `companyType` field to the org-profile schemas (read, write, claim, settings) and `OrgProfileView`, mirroring `companySize`. The self-declared organisation form; registry `legalForm` and AI-derived type are layered separately by the AppView.
