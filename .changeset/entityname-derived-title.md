---
'@singi-labs/sifa-sdk': patch
---

Prefer the resolved company `entityName` over the free-text `company` snapshot when deriving the meta description and JSON-LD `worksFor`. A corrected (or decoded) company name now shows in the profile's link preview and structured data instead of the value frozen into the position record.
