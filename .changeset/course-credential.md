---
'@singi-labs/sifa-sdk': patch
---

Add optional `credential` (at-uri) to `ProfileCourseRecordSchema` and `credentialRkey` to the `ProfileCourse` view type, so a course can reference the certification it earned. Mirrors `id.sifa.profile.course` lexicon 0.7.1.
