---
'@singi-labs/sifa-sdk': patch
---

Add `fetchMyGithubPullRequests` fetcher and `useMyGithubPullRequests` hook (under the `/query` subpath) for listing the authenticated viewer's own ingested merged GitHub PRs. Backs the sifa-web GitHub importer.
