---
'@singi-labs/sifa-sdk': patch
---

Add Guestbook (dev.baileytownsend.guestbook) to the app category map and collection-to-app mapping. Entries render non-clickable (self-hosted widget, no public per-record viewer).
