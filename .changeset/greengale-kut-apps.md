---
'@singi-labs/sifa-sdk': patch
---

Add GreenGale (blog posts) and Kut (community memberships) to the app category map, activity tiers, and URL patterns. GreenGale entries link to `/{handle}/{rkey}`; Kut memberships link to the joined community, parsed from the membership's `community.uri`.
