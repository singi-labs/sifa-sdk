---
'@singi-labs/sifa-sdk': patch
---

`buildMetaDescription` no longer repeats the current-role line when the headline already equals it, so a profile whose headline is "Title at Company" gets a single clause instead of "Title at Company · Title at Company".
