---
'@singi-labs/sifa-sdk': patch
---

Add a `Translations` app category (Phosphor `Translate`) and map two new apps: `locale` (localization projects and translation strings on locale.at) and `pckt` (microblog posts that have no `site.standard.document` twin).
