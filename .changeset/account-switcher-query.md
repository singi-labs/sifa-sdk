---
'@singi-labs/sifa-sdk': patch
---

Add the account-switcher query layer: `fetchAccounts` / `useAccounts` (list the accounts attached to the browser), the `switchAccount` fetcher (POST to activate one by DID), and the `AccountSummary` type.
