---
'@singi-labs/sifa-sdk': patch
---

Add `revealMarqueDomain` / `unrevealMarqueDomain` fetchers and the `useRevealMarqueDomain` / `useUnrevealMarqueDomain` mutation hooks to the query layer, so sifa-web can toggle the public visibility of Marque-registered domains in the profile Links section.
