---
'@singi-labs/sifa-sdk': patch
---

Add `fediverse` to `APP_CATEGORY_MAP` (category `Posts`). Ingested Fediverse posts become a first-class app in the registry so users can toggle them off in their activity settings (sifa-workspace#378).
