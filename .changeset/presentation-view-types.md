---
'@singi-labs/sifa-sdk': patch
---

Add `ProfilePresentation`, `ProfilePresentationDelivery`, and `PresentationLinkView` view-model types, and the `presentations` / `presentationDeliveries` fields on `Profile`, for the Talks & sessions profile section.
