---
'@singi-labs/sifa-sdk': patch
---

Add `Domains` app category (Globe icon) and map the `marque` app id to it, so Marque domain-registration activity is categorized instead of falling through as uncategorized.
