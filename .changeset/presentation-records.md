---
'@singi-labs/sifa-sdk': patch
---

Add `id.sifa.profile.presentation` and `id.sifa.profile.presentationDelivery` record schemas (the Talks & sessions content and occasion records), the `externalRecordRef` shared schema, presentation role / link-type / calendar-event taxonomy label maps, and a `formatPresentationDuration` helper.
