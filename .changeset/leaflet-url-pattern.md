---
'@singi-labs/sifa-sdk': patch
---

Add Leaflet (`pub.leaflet.*`) URL patterns and collection-to-app mapping. Per-document URL is `https://leaflet.pub/{rkey}`; profile fallback links to the marketing site since leaflet.pub has no per-user profile route. Consumed by sifa-web's upcoming leaflet activity card.
