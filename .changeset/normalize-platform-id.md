---
'@singi-labs/sifa-sdk': patch
---

Add `normalizePlatformId` to map lexicon platform tokens (`id.sifa.defs#platformLinkedin`) to short codes (`linkedin`); `getPlatformLabel` now normalizes first so token-form external accounts render the correct label.
