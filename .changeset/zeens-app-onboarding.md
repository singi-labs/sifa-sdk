---
'@singi-labs/sifa-sdk': patch
---

Recognise Zeens (zeens.app) photo collections. Adds the `app.photosky.` prefix to `COLLECTION_TO_APP`, the `zeens` URL patterns, the `Photos` category mapping, and a `creation` activity tier for `app.photosky.collection`.
