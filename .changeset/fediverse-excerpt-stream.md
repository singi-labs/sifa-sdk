---
'@singi-labs/sifa-sdk': patch
---

Read `excerpt` as a generic stream-card text field, so external-feed items (Fediverse `fediverse.post`, RSS) show their post text on the activity stream instead of falling back to a bare link.
