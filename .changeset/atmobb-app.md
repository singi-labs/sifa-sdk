---
'@singi-labs/sifa-sdk': patch
---

Add atmoBB to the app category map and card-URL resolver. Forum threads and replies (`app.atmobb.discussion.*`) now resolve to their atmobb.app viewer pages (`/t/{did}/{rkey}`), with replies linking to their parent thread.
